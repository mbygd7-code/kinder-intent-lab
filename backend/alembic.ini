; NOTE: alembic reads this file with the OS locale encoding - keep it ASCII-only.
[alembic]
script_location = %(here)s/../db/migrations
; URL comes from DATABASE_URL / TEST_DATABASE_URL env vars via env.py (no secrets here)
sqlalchemy.url =
