# Runtime Integration Spec v0.2 (PARTIAL HOLD)

원본 파일: 상위 폴더 `outputs/docs/06-runtime-integration/kinder-intent-runtime-spec-v0.1.md` (내용은 v0.2).

이 폴더를 저장소로 옮길 때 위 원본 파일을 여기로 복사할 것.
live rollout(Suggest/Assist/Auto) 구현은 KTIB 80% + 안전 지표 충족 전까지 금지.
현재 개발에서 참조하는 부분: §2(Stage 0a/0b 거버넌스 게이트) · §3-0(Core Contract) · §3-1(infer 계약 — schemas/에 반영됨) · §4(decision 정책).
